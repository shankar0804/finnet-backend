# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
service_account.json
venv/
.venv/
whatsapp-bot/auth_info/

# Environment variables
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project specific
deploy/
win-venv/
**/__pycache__/
client_secrets.json
service_account.json

# Secrets / deployment notes (contains Supabase service key, NVIDIA keys,
# APIFY token, JWT secret, Google SA — NEVER commit)
RENDER_ENV_VARS.txt

# Local-only tools / artifacts
supabase.exe
server.log

# Frontend is a separate repo (finnet-frontend) — never commit to backend
frontend/
_frontend_deploy/
